package api;

import android.media.Image;
import java.util.Date;
import layout.UserProfile;

public class EventCreateAPI {
    public int createEvent(int eventType, String title, String description, Date datetime, String location, Image image,
                           boolean hasRSVP, UserProfile organiser) {
        int id = 0;

        return id;
    }
}
